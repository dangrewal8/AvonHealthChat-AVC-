# ============================================================================
# Avon Health RAG Backend - Environment Variables Template
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in all the values below with your actual credentials
# 3. NEVER commit .env to git (it's already in .gitignore)
#
# ============================================================================

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------
NODE_ENV=production
PORT=3001

# ----------------------------------------------------------------------------
# Avon Health API Credentials (REQUIRED)
# ----------------------------------------------------------------------------
# Get these from your Avon Health account
# SECURITY: NEVER share these credentials or commit them to git!

AVON_CLIENT_ID=your_client_id_here
AVON_CLIENT_SECRET=your_client_secret_here
AVON_BASE_URL=https://demo-api.avonhealth.com
AVON_ACCOUNT=your_account_here
AVON_USER_ID=your_user_id_here

# ----------------------------------------------------------------------------
# CORS Configuration (REQUIRED FOR CLOUDFLARE TUNNEL)
# ----------------------------------------------------------------------------
# Set to your frontend domain in production
# Examples:
#   Development: http://localhost:5173
#   Production: https://chat.yourdomain.com
#   Multiple: https://chat.yourdomain.com,https://app.yourdomain.com
#
# IMPORTANT: Replace 'yourdomain.com' with your actual domain!

CORS_ORIGIN=https://chat.yourdomain.com

# ----------------------------------------------------------------------------
# Ollama Configuration (Optional - for RAG)
# ----------------------------------------------------------------------------
# Leave as defaults if Ollama is running locally
# Set to remote URL if using remote Ollama instance

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
OLLAMA_LLM_MODEL=meditron
OLLAMA_MAX_TOKENS=4096
OLLAMA_TEMPERATURE=0.1

# ----------------------------------------------------------------------------
# Vector Database Configuration (Optional)
# ----------------------------------------------------------------------------
VECTOR_DB_TYPE=faiss
FAISS_DIMENSION=768
FAISS_INDEX_PATH=./data/faiss

# ----------------------------------------------------------------------------
# Cache Configuration (Optional)
# ----------------------------------------------------------------------------
CACHE_ENABLED=false
CACHE_TTL_SECONDS=300

# ----------------------------------------------------------------------------
# Rate Limiting (Security)
# ----------------------------------------------------------------------------
# Number of requests allowed per window
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
