# Frontend Production Environment Configuration
# Avon Health RAG System
#
# In production with Docker, the frontend uses nginx which proxies
# API requests to the backend container, so we use a relative URL

# API Base URL (relative, proxied by nginx)
VITE_API_BASE_URL=/

# Application Info
VITE_APP_NAME=Avon Health RAG System
VITE_APP_VERSION=1.0.0

# Features
VITE_ENABLE_STREAMING=false
